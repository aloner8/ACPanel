# Docker Infrastructure

This folder will contain local and production container definitions for:

- PostgreSQL
- Node.js API
- Angular frontend / Nginx UI hosting
- optional helper services

Suggested next file:

- `docker-compose.yml` for local development
